spring:
  profiles:
    active:
      - dev

---

spring:
  profiles: dev
  application:
    name: microservicecloud-config-zlsoft-dev

---

spring:
  profiles: test
  application:
    name: microservicecloud-config-zlsoft-test